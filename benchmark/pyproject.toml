[project]
name = "benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "raulsecante1", email = "raulsecante1@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
benchmark = "benchmark.main:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
