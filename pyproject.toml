[project]
name = "itisamanager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "raulsecante1", email = "raulsecante1@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.15",
    "langchain-openrouter>=0.2.8",
    "langchain-text-splitters>=1.1.2",
    "pydantic-settings>=2.15.0",
    "tiktoken>=0.14.0",
]

[project.scripts]
itisamanager = "itisamanager.main:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
